# AI service dependencies. The Render image also needs shared backend
# dependencies for JWT validation and the existing PostgreSQL user lookup.
-r requirements.txt

google-genai==1.0.0
pgvector==0.3.6
sentence-transformers==3.0.1
